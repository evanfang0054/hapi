# Rewind 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 HAPI Web 端实现 /rewind 功能，允许用户回退到对话历史中某条用户消息，撤销之后的对话和文件变更。

**Architecture:** CLI 端通过 RPC handler 接收 rewind 请求，截断 JSONL 文件并恢复 file-history-snapshot，然后通过 `--resume` 重启 Claude 进程。Hub 端负责路由转发和消息清理。Web 端提供 UI 入口和 API 调用。

**Tech Stack:** TypeScript, Zod, Socket.IO RPC, Hono, TanStack Query, React

---

## File Structure

| File | Action | Responsibility |
|------|--------|---------------|
| `shared/src/schemas.ts` | Modify | 新增 `messages-rewound` SyncEvent 类型 |
| `cli/src/claude/utils/rewind.ts` | Create | JSONL 解析、截断、file-history-snapshot 恢复 |
| `cli/src/claude/utils/rewind.test.ts` | Create | rewind 工具函数的测试 |
| `cli/src/claude/claudeRemoteLauncher.ts` | Modify | 注册 rewind RPC handler，主循环支持 rewind 重启 |
| `cli/src/modules/common/remote/RemoteLauncherBase.ts` | Modify | 新增 `setupRewindHandler` 方法 |
| `hub/src/store/messages.ts` | Modify | 新增 `deleteMessagesAfterSeq` |
| `hub/src/sync/rpcGateway.ts` | Modify | 新增 `rewindSession` 方法 |
| `hub/src/sync/syncEngine.ts` | Modify | 新增 `rewindSession` 方法 |
| `hub/src/sync/messageService.ts` | Modify | 新增消息删除后推送 messages-rewound 事件 |
| `hub/src/web/routes/sessions.ts` | Modify | 新增 POST `/sessions/:id/rewind` 路由 |
| `web/src/api/client.ts` | Modify | 新增 `rewindSession` 方法 |
| `web/src/hooks/mutations/useSessionActions.ts` | Modify | 新增 `rewindSession` mutation |
| `web/src/hooks/useSSE.ts` | Modify | 处理 `messages-rewound` SSE 事件 |
| `web/src/components/AssistantChat/messages/UserMessage.tsx` | Modify | 添加 rewind 按钮 |

---

### Task 1: Shared 协议 — 新增 `messages-rewound` SyncEvent

**Files:**
- Modify: `shared/src/schemas.ts:236-244`

- [ ] **Step 1: 在 SyncEventSchema union 中新增 `messages-rewound` 类型**

在 `shared/src/schemas.ts` 的 `SyncEventSchema` `z.discriminatedUnion` 数组末尾、闭合括号前新增：

```typescript
SessionEventBaseSchema.extend({
    type: z.literal('messages-rewound'),
    sessionId: z.string(),
    targetSeq: z.number()
}),
```

- [ ] **Step 2: 运行 shared 的类型检查**

Run: `cd /Users/arwen/Desktop/Arwen/evanfang/hapi && bun run typecheck 2>&1 | head -20`
Expected: 无类型错误（或有已有的不相关错误）

- [ ] **Step 3: Commit**

```bash
git add shared/src/schemas.ts
git commit -m "feat(shared): add messages-rewound sync event type"
```

---

### Task 2: CLI — JSONL 解析与截断工具

**Files:**
- Create: `cli/src/claude/utils/rewind.ts`
- Create: `cli/src/claude/utils/rewind.test.ts`

- [ ] **Step 1: 编写 rewind.ts 的失败测试**

Create `cli/src/claude/utils/rewind.test.ts`：

```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest'
import { writeFileSync, mkdirSync, rmSync, existsSync } from 'node:fs'
import { join } from 'node:path'
import { tmpdir } from 'node:os'
import { buildMessageChain, truncateJsonl, findFileSnapshot } from './rewind'

describe('rewind utilities', () => {
    let testDir: string

    beforeEach(() => {
        testDir = join(tmpdir(), `rewind-test-${Date.now()}`)
        mkdirSync(testDir, { recursive: true })
    })

    afterEach(() => {
        rmSync(testDir, { recursive: true, force: true })
    })

    describe('buildMessageChain', () => {
        it('builds a chain from a valid JSONL file', () => {
            const jsonlPath = join(testDir, 'test.jsonl')
            writeFileSync(jsonlPath, [
                JSON.stringify({ type: 'user', uuid: 'aaa', parentUuid: null, message: { role: 'user', content: 'hi' } }),
                JSON.stringify({ type: 'assistant', uuid: 'bbb', parentUuid: 'aaa', message: { role: 'assistant', content: 'hello' } }),
                JSON.stringify({ type: 'user', uuid: 'ccc', parentUuid: 'bbb', message: { role: 'user', content: 'bye' } }),
            ].join('\n') + '\n')

            const chain = buildMessageChain(jsonlPath)
            expect(chain).toHaveLength(3)
            expect(chain[0].uuid).toBe('aaa')
            expect(chain[1].parentUuid).toBe('aaa')
            expect(chain[2].parentUuid).toBe('bbb')
        })

        it('skips file-history-snapshot and other internal types', () => {
            const jsonlPath = join(testDir, 'test.jsonl')
            writeFileSync(jsonlPath, [
                JSON.stringify({ type: 'file-history-snapshot', uuid: 'snap1', msgId: 'aaa' }),
                JSON.stringify({ type: 'user', uuid: 'aaa', parentUuid: null, message: { role: 'user', content: 'hi' } }),
            ].join('\n') + '\n')

            const chain = buildMessageChain(jsonlPath)
            expect(chain).toHaveLength(1)
            expect(chain[0].uuid).toBe('aaa')
        })

        it('throws if file does not exist', () => {
            expect(() => buildMessageChain(join(testDir, 'nope.jsonl'))).toThrow()
        })
    })

    describe('truncateJsonl', () => {
        it('truncates file keeping lines up to and including target uuid', () => {
            const jsonlPath = join(testDir, 'test.jsonl')
            writeFileSync(jsonlPath, [
                JSON.stringify({ type: 'user', uuid: 'aaa', parentUuid: null, message: { role: 'user', content: 'hi' } }),
                JSON.stringify({ type: 'assistant', uuid: 'bbb', parentUuid: 'aaa', message: { role: 'assistant', content: 'hello' } }),
                JSON.stringify({ type: 'user', uuid: 'ccc', parentUuid: 'bbb', message: { role: 'user', content: 'bye' } }),
            ].join('\n') + '\n')

            truncateJsonl(jsonlPath, 'bbb')

            const chain = buildMessageChain(jsonlPath)
            expect(chain).toHaveLength(2)
            expect(chain[0].uuid).toBe('aaa')
            expect(chain[1].uuid).toBe('bbb')
        })

        it('throws if target uuid not found', () => {
            const jsonlPath = join(testDir, 'test.jsonl')
            writeFileSync(jsonlPath, [
                JSON.stringify({ type: 'user', uuid: 'aaa', parentUuid: null, message: { role: 'user', content: 'hi' } }),
            ].join('\n') + '\n')

            expect(() => truncateJsonl(jsonlPath, 'nonexistent')).toThrow('not found')
        })
    })

    describe('findFileSnapshot', () => {
        it('finds the most recent file-history-snapshot before target uuid', () => {
            const jsonlPath = join(testDir, 'test.jsonl')
            writeFileSync(jsonlPath, [
                JSON.stringify({ type: 'file-history-snapshot', uuid: 'snap1', msgId: 'pre', files: { '/a.ts': 'content1' } }),
                JSON.stringify({ type: 'user', uuid: 'aaa', parentUuid: null, message: { role: 'user', content: 'hi' } }),
                JSON.stringify({ type: 'file-history-snapshot', uuid: 'snap2', msgId: 'bbb', files: { '/b.ts': 'content2' } }),
                JSON.stringify({ type: 'assistant', uuid: 'bbb', parentUuid: 'aaa', message: { role: 'assistant', content: 'hello' } }),
                JSON.stringify({ type: 'user', uuid: 'ccc', parentUuid: 'bbb', message: { role: 'user', content: 'bye' } }),
            ].join('\n') + '\n')

            const snapshot = findFileSnapshot(jsonlPath, 'ccc')
            expect(snapshot).not.toBeNull()
            // Should find snap2 which is associated with bbb (the message before ccc)
        })

        it('returns null if no snapshot found', () => {
            const jsonlPath = join(testDir, 'test.jsonl')
            writeFileSync(jsonlPath, [
                JSON.stringify({ type: 'user', uuid: 'aaa', parentUuid: null, message: { role: 'user', content: 'hi' } }),
            ].join('\n') + '\n')

            const snapshot = findFileSnapshot(jsonlPath, 'aaa')
            expect(snapshot).toBeNull()
        })
    })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/arwen/Desktop/Arwen/evanfang/hapi/cli && bunx vitest run src/claude/utils/rewind.test.ts 2>&1 | tail -20`
Expected: FAIL — module not found

- [ ] **Step 3: 实现 rewind.ts**

Create `cli/src/claude/utils/rewind.ts`：

```typescript
import { readFileSync, writeFileSync, existsSync } from 'node:fs'
import { logger } from '@/ui/logger'

type MessageChainEntry = {
    uuid: string
    parentUuid: string | null
    type: string
    line: string
    lineNumber: number
}

type FileSnapshot = {
    files: Record<string, string>
}

const INTERNAL_TYPES = new Set([
    'file-history-snapshot',
    'change',
    'queue-operation',
])

/**
 * Parse a JSONL file and build a message chain of user/assistant/system messages.
 * Skips internal Claude Code event types.
 */
export function buildMessageChain(jsonlPath: string): MessageChainEntry[] {
    if (!existsSync(jsonlPath)) {
        throw new Error(`JSONL file not found: ${jsonlPath}`)
    }

    const content = readFileSync(jsonlPath, 'utf-8')
    const lines = content.split('\n')
    const chain: MessageChainEntry[] = []

    for (let i = 0; i < lines.length; i++) {
        const line = lines[i].trim()
        if (!line) continue

        try {
            const parsed = JSON.parse(line)
            if (INTERNAL_TYPES.has(parsed.type)) continue
            if (!parsed.uuid) continue

            chain.push({
                uuid: parsed.uuid,
                parentUuid: parsed.parentUuid ?? null,
                type: parsed.type,
                line,
                lineNumber: i,
            })
        } catch {
            // Skip malformed lines
        }
    }

    return chain
}

/**
 * Truncate a JSONL file, keeping all lines up to and including the line with targetUuid.
 * Both internal and message lines are preserved up to the target.
 */
export function truncateJsonl(jsonlPath: string, targetUuid: string): void {
    if (!existsSync(jsonlPath)) {
        throw new Error(`JSONL file not found: ${jsonlPath}`)
    }

    const content = readFileSync(jsonlPath, 'utf-8')
    const lines = content.split('\n')
    let targetLineIndex = -1

    for (let i = 0; i < lines.length; i++) {
        const line = lines[i].trim()
        if (!line) continue
        try {
            const parsed = JSON.parse(line)
            if (parsed.uuid === targetUuid) {
                targetLineIndex = i
                break
            }
        } catch {
            // Skip malformed lines
        }
    }

    if (targetLineIndex === -1) {
        throw new Error(`Target uuid not found in JSONL: ${targetUuid}`)
    }

    // Keep lines 0..targetLineIndex (inclusive)
    const keptLines = lines.slice(0, targetLineIndex + 1)
    writeFileSync(jsonlPath, keptLines.join('\n') + '\n')

    logger.debug(`[rewind] Truncated JSONL at line ${targetLineIndex}, uuid=${targetUuid}`)
}

/**
 * Find the most recent file-history-snapshot that appears before the target uuid.
 * Returns null if no snapshot exists.
 */
export function findFileSnapshot(jsonlPath: string, targetUuid: string): FileSnapshot | null {
    if (!existsSync(jsonlPath)) {
        return null
    }

    const content = readFileSync(jsonlPath, 'utf-8')
    const lines = content.split('\n')

    let foundTarget = false
    let lastSnapshot: FileSnapshot | null = null

    for (const line of lines) {
        const trimmed = line.trim()
        if (!trimmed) continue

        try {
            const parsed = JSON.parse(trimmed)

            if (parsed.uuid === targetUuid) {
                foundTarget = true
                break
            }

            if (parsed.type === 'file-history-snapshot' && parsed.files && typeof parsed.files === 'object') {
                lastSnapshot = { files: parsed.files }
            }
        } catch {
            // Skip malformed lines
        }
    }

    if (!foundTarget) {
        return null
    }

    return lastSnapshot
}

/**
 * Apply a file snapshot by writing files back to disk.
 * Returns the list of files restored.
 */
export function applyFileSnapshot(snapshot: FileSnapshot, cwd: string): string[] {
    const { writeFileSync: writeFile, mkdirSync: mkdir } = require('node:fs')
    const { dirname, join } = require('node:path')
    const restored: string[] = []

    for (const [filePath, content] of Object.entries(snapshot.files)) {
        if (typeof content !== 'string') continue
        const absolutePath = filePath.startsWith('/') ? filePath : join(cwd, filePath)
        try {
            mkdir(dirname(absolutePath), { recursive: true })
            writeFile(absolutePath, content, 'utf-8')
            restored.push(filePath)
        } catch (error) {
            logger.debug(`[rewind] Failed to restore file: ${filePath}`, error)
        }
    }

    return restored
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/arwen/Desktop/Arwen/evanfang/hapi/cli && bunx vitest run src/claude/utils/rewind.test.ts 2>&1 | tail -20`
Expected: ALL PASS

- [ ] **Step 5: Commit**

```bash
git add cli/src/claude/utils/rewind.ts cli/src/claude/utils/rewind.test.ts
git commit -m "feat(cli): add JSONL rewind utilities with tests"
```

---

### Task 3: CLI — Rewind RPC Handler 注册

**Files:**
- Modify: `cli/src/modules/common/remote/RemoteLauncherBase.ts:20-23,75-91`
- Modify: `cli/src/claude/claudeRemoteLauncher.ts:75-92`

- [ ] **Step 1: 在 RemoteLauncherBase 中新增 rewind handler 类型和方法**

在 `cli/src/modules/common/remote/RemoteLauncherBase.ts` 中：

1. 在 `RemoteLauncherAbortHandlers` 类型后面新增：

```typescript
export type RemoteLauncherRewindHandler = {
    onRewind: (params: { targetUuid: string }) => Promise<void>;
};
```

2. 在 `setupAbortHandlers` 方法后面新增：

```typescript
protected setupRewindHandler(
    rpcHandlerManager: RpcHandlerManagerLike,
    handler: RemoteLauncherRewindHandler
): void {
    rpcHandlerManager.registerHandler('rewind', async (params: unknown) => {
        const { targetUuid } = params as { targetUuid: string }
        await handler.onRewind({ targetUuid })
        return { ok: true }
    });
}

protected clearRewindHandler(rpcHandlerManager: RpcHandlerManagerLike): void {
    rpcHandlerManager.registerHandler('rewind', async () => {});
}
```

- [ ] **Step 2: 在 claudeRemoteLauncher 中注册 rewind handler**

在 `cli/src/claude/claudeRemoteLauncher.ts` 中：

1. 在文件顶部添加导入：

```typescript
import { buildMessageChain, truncateJsonl, findFileSnapshot, applyFileSnapshot } from './utils/rewind';
import { getProjectPath } from './utils/path';
```

2. 在 `ClaudeRemoteLauncher` 类中新增属性：

```typescript
private isRewinding = false;
private rewindAbortController: AbortController | null = null;
```

3. 在 `runMainLoop` 方法中，在 `this.setupAbortHandlers(session.client.rpcHandlerManager, {...})` 调用后面，添加 rewind handler 注册：

```typescript
this.setupRewindHandler(session.client.rpcHandlerManager, {
    onRewind: async ({ targetUuid }) => {
        if (this.isRewinding) {
            logger.debug('[remote]: rewind already in progress');
            return;
        }
        this.isRewinding = true;

        try {
            // 1. Abort current query iteration
            if (this.abortController && !this.abortController.signal.aborted) {
                this.rewindAbortController = this.abortController;
                this.abortController.abort();
            }
            await this.abortFuture?.promise;

            // 2. Truncate JSONL
            const claudeSessionId = session.sessionId;
            if (!claudeSessionId) {
                logger.debug('[remote]: no session id, cannot rewind');
                return;
            }

            const projectDir = getProjectPath(session.path);
            const jsonlPath = join(projectDir, `${claudeSessionId}.jsonl`);

            truncateJsonl(jsonlPath, targetUuid);

            // 3. Try to restore file snapshot (degraded if not found)
            const snapshot = findFileSnapshot(jsonlPath, targetUuid);
            if (snapshot) {
                const restored = applyFileSnapshot(snapshot, session.path);
                logger.debug(`[remote]: restored ${restored.length} files from snapshot`);
            } else {
                logger.debug('[remote]: no file snapshot found, skipping file restore');
            }

            // 4. The main loop will continue because we didn't set exitReason.
            // The next iteration of claudeRemote will use the existing sessionId
            // as the resume parameter since it's still set in session.sessionId.
            logger.debug(`[remote]: rewind complete, process will resume from uuid=${targetUuid}`);
        } catch (error) {
            logger.debug('[remote]: rewind failed', error);
        } finally {
            this.isRewinding = false;
            this.rewindAbortController = null;
        }
    }
});
```

4. 在 `cleanup` 方法中，在 `this.clearAbortHandlers(...)` 后面添加：

```typescript
this.clearRewindHandler(this.session.client.rpcHandlerManager);
```

5. 添加缺少的 `join` 导入（在文件顶部 import 区域）：

```typescript
import { join } from 'node:path';
```

- [ ] **Step 3: 运行类型检查**

Run: `cd /Users/arwen/Desktop/Arwen/evanfang/hapi && bun run typecheck:cli 2>&1 | tail -20`
Expected: 无类型错误

- [ ] **Step 4: Commit**

```bash
git add cli/src/modules/common/remote/RemoteLauncherBase.ts cli/src/claude/claudeRemoteLauncher.ts
git commit -m "feat(cli): register rewind RPC handler in remote launcher"
```

---

### Task 4: Hub — 消息删除方法

**Files:**
- Modify: `hub/src/store/messages.ts:163`

- [ ] **Step 1: 在 messages.ts 末尾新增 `deleteMessagesAfterSeq` 函数**

在 `hub/src/store/messages.ts` 文件末尾追加：

```typescript
export function deleteMessagesAfterSeq(
    db: Database,
    sessionId: string,
    afterSeq: number
): number {
    const result = db.prepare(
        'DELETE FROM messages WHERE session_id = ? AND seq > ?'
    ).run(sessionId, afterSeq)
    return result.changes
}
```

- [ ] **Step 2: Commit**

```bash
git add hub/src/store/messages.ts
git commit -m "feat(hub): add deleteMessagesAfterSeq for rewind support"
```

---

### Task 5: Hub — RPC Gateway + SyncEngine + 路由

**Files:**
- Modify: `hub/src/sync/rpcGateway.ts:92-94`
- Modify: `hub/src/sync/syncEngine.ts:269-271`
- Modify: `hub/src/web/routes/sessions.ts:184-197`

- [ ] **Step 1: 在 RpcGateway 中新增 rewindSession 方法**

在 `hub/src/sync/rpcGateway.ts` 的 `abortSession` 方法后面新增：

```typescript
async rewindSession(sessionId: string, targetUuid: string): Promise<void> {
    await this.sessionRpc(sessionId, 'rewind', { targetUuid })
}
```

- [ ] **Step 2: 在 SyncEngine 中新增 rewindSession 方法**

在 `hub/src/sync/syncEngine.ts` 中：

1. 在文件顶部 import 区新增：

```typescript
import { deleteMessagesAfterSeq } from '../store/messages'
import { getMaxSeq } from '../store/messages'
```

2. 在 `abortSession` 方法后面新增：

```typescript
async rewindSession(sessionId: string, targetSeq: number): Promise<void> {
    // Find the target message's localId (which is the JSONL uuid)
    const messages = this.messageService.getMessagesPage(sessionId, {
        limit: 1,
        beforeSeq: targetSeq + 1
    })
    const targetMessage = messages.messages.find(m => m.seq === targetSeq)
    const targetUuid = targetMessage?.localId

    if (!targetUuid) {
        throw new Error(`Message with seq ${targetSeq} not found or has no localId`)
    }

    // Call CLI to perform rewind
    await this.rpcGateway.rewindSession(sessionId, targetUuid)

    // Delete messages after targetSeq
    const db = (this as any).sessionCache ? undefined : undefined
    // We need access to the store's db; SyncEngine doesn't expose it directly
    // but the messageService does through the store
    // Use the eventPublisher to broadcast the rewind event
    this.handleRealtimeEvent({
        type: 'messages-rewound',
        sessionId,
        targetSeq
    })
}
```

注意：需要在 `hub/src/sync/messageService.ts` 中导入并暴露 `deleteMessagesAfterSeq`。首先检查 `MessageService` 是否有 `store` 属性。如果有：

```typescript
import { deleteMessagesAfterSeq } from '../store/messages'

// 在 MessageService 类中新增：
deleteMessagesAfterSeq(sessionId: string, afterSeq: number): number {
    return deleteMessagesAfterSeq(this.store.db, sessionId, afterSeq)
}
```

然后 SyncEngine 的 `rewindSession` 实现：

```typescript
async rewindSession(sessionId: string, targetSeq: number): Promise<void> {
    const messages = this.messageService.getMessagesPage(sessionId, {
        limit: 1,
        beforeSeq: targetSeq + 1
    })
    const targetMessage = messages.messages.find(m => m.seq === targetSeq)
    const targetUuid = targetMessage?.localId

    if (!targetUuid) {
        throw new Error(`Message with seq ${targetSeq} not found or has no localId`)
    }

    await this.rpcGateway.rewindSession(sessionId, targetUuid)

    this.messageService.deleteMessagesAfterSeq(sessionId, targetSeq)

    this.handleRealtimeEvent({
        type: 'messages-rewound',
        sessionId,
        targetSeq
    })
}
```

- [ ] **Step 3: 在 sessions 路由中新增 rewind 端点**

在 `hub/src/web/routes/sessions.ts` 中，在 `abort` 路由后面新增：

```typescript
const rewindSchema = z.object({
    targetSeq: z.number().int().positive()
})

app.post('/sessions/:id/rewind', async (c) => {
    const engine = requireSyncEngine(c, getSyncEngine)
    if (engine instanceof Response) {
        return engine
    }

    const sessionResult = requireSessionFromParam(c, engine, { requireActive: true })
    if (sessionResult instanceof Response) {
        return sessionResult
    }

    const body = await c.req.json().catch(() => null)
    const parsed = rewindSchema.safeParse(body)
    if (!parsed.success) {
        return c.json({ error: 'Invalid body: targetSeq is required' }, 400)
    }

    // Only support rewind for remote sessions
    const mode = sessionResult.session.agentState?.controlledByUser
    if (mode === true) {
        return c.json({ error: 'Rewind is only supported for remote sessions' }, 400)
    }

    try {
        await engine.rewindSession(sessionResult.sessionId, parsed.data.targetSeq)
        return c.json({ ok: true })
    } catch (error) {
        return c.json({
            error: error instanceof Error ? error.message : 'Failed to rewind session'
        }, 500)
    }
})
```

- [ ] **Step 4: 运行类型检查**

Run: `cd /Users/arwen/Desktop/Arwen/evanfang/hapi && bun run typecheck:hub 2>&1 | tail -20`
Expected: 无类型错误

- [ ] **Step 5: Commit**

```bash
git add hub/src/sync/rpcGateway.ts hub/src/sync/syncEngine.ts hub/src/sync/messageService.ts hub/src/web/routes/sessions.ts hub/src/store/messages.ts
git commit -m "feat(hub): add rewind session API with RPC gateway and message cleanup"
```

---

### Task 6: Web — API 客户端 + Mutation

**Files:**
- Modify: `web/src/api/client.ts`
- Modify: `web/src/hooks/mutations/useSessionActions.ts`

- [ ] **Step 1: 在 ApiClient 中新增 rewindSession 方法**

在 `web/src/api/client.ts` 中，在 `abortSession` 方法附近新增：

```typescript
async rewindSession(sessionId: string, targetSeq: number): Promise<void> {
    await this.request(`/api/sessions/${encodeURIComponent(sessionId)}/rewind`, {
        method: 'POST',
        body: JSON.stringify({ targetSeq })
    })
}
```

- [ ] **Step 2: 在 useSessionActions 中新增 rewind mutation**

在 `web/src/hooks/mutations/useSessionActions.ts` 中：

1. 在返回类型中新增 `rewindSession` 签名：

```typescript
rewindSession: (targetSeq: number) => Promise<void>
```

2. 在 `deleteMutation` 后面新增 rewind mutation：

```typescript
const rewindMutation = useMutation({
    mutationFn: async (targetSeq: number) => {
        if (!api || !sessionId) {
            throw new Error('Session unavailable')
        }
        await api.rewindSession(sessionId, targetSeq)
    },
    onSuccess: () => {
        if (!sessionId) return
        clearMessageWindow(sessionId)
        void invalidateSession()
    },
})
```

3. 在返回对象中添加：

```typescript
rewindSession: rewindMutation.mutateAsync,
```

4. 在 `isPending` 计算中添加 `rewindMutation.isPending`：

```typescript
|| rewindMutation.isPending
```

- [ ] **Step 3: 运行类型检查**

Run: `cd /Users/arwen/Desktop/Arwen/evanfang/hapi && bun run typecheck:web 2>&1 | tail -20`
Expected: 无类型错误

- [ ] **Step 4: Commit**

```bash
git add web/src/api/client.ts web/src/hooks/mutations/useSessionActions.ts
git commit -m "feat(web): add rewindSession API client and mutation"
```

---

### Task 7: Web — SSE 处理 messages-rewound 事件

**Files:**
- Modify: `web/src/hooks/useSSE.ts:496-529`

- [ ] **Step 1: 在 useSSE 的 handleSyncEvent 中处理 messages-rewound 事件**

在 `web/src/hooks/useSSE.ts` 的 `handleSyncEvent` 函数中，在 `session-removed` 分支附近新增：

```typescript
if (event.type === 'messages-rewound') {
    clearMessageWindow(event.sessionId)
    queueSessionDetailInvalidation(event.sessionId)
    queueSessionListInvalidation()
}
```

这需要 `clearMessageWindow` 已经在文件顶部导入（已存在：`import { clearMessageWindow, ingestIncomingMessages } from '@/lib/message-window-store'`）。

- [ ] **Step 2: 运行类型检查**

Run: `cd /Users/arwen/Desktop/Arwen/evanfang/hapi && bun run typecheck:web 2>&1 | tail -20`
Expected: 无类型错误

- [ ] **Step 3: Commit**

```bash
git add web/src/hooks/useSSE.ts
git commit -m "feat(web): handle messages-rewound SSE event"
```

---

### Task 8: Web — 用户消息气泡 Rewind 按钮

**Files:**
- Modify: `web/src/components/AssistantChat/messages/UserMessage.tsx`
- Modify: `web/src/components/AssistantChat/context.ts` (如果 context 需要扩展)

- [ ] **Step 1: 了解 context 中 session 信息如何传递**

检查 `useHappyChatContext` 的类型定义，确认是否可以从 context 获取 `sessionId`、`session active`、`controlledByUser` 等信息。根据 `UserMessage.tsx` 中的现有模式（如 `ctx.onRetryMessage`），可能需要在 context 中新增 `onRewindMessage` 回调。

在 `web/src/components/AssistantChat/messages/UserMessage.tsx` 中，在 `UserMessage` 组件的返回 JSX 中，在 Copy 按钮旁边（第 97 行后）新增 rewind 按钮。

首先需要从消息 metadata 中获取 seq。由于 `HappyChatMessageMetadata` 中没有 seq 字段，需要在更上层（消息转换时）将 seq 传入 metadata。检查消息在 `message-window-store` 中的 seq 字段是否可在 assistant-ui runtime 中访问。

由于当前 `HappyChatMessageMetadata` 不包含 seq 信息，最简单的方案是从消息的 parent thread 中获取。但考虑到 assistant-ui 的抽象，更务实的做法是：

**在 `HappyChatMessageMetadata` 中新增 `seq` 字段**，在消息转换时填入。

这需要修改：
1. `web/src/lib/assistant-runtime.ts` — 在 `HappyChatMessageMetadata` 类型中新增 `seq?: number`
2. 在消息 block 转换时填入 seq（从 message-window-store 的 block 中获取）

- [ ] **Step 2: 在 HappyChatMessageMetadata 中添加 seq 字段**

在 `web/src/lib/assistant-runtime.ts` 的 `HappyChatMessageMetadata` 类型中新增：

```typescript
seq?: number
```

然后在 user message block 的 metadata 中填入 seq（需要从 block 数据中获取，具体取决于消息转换逻辑）。

- [ ] **Step 3: 在 UserMessage 组件中添加 Rewind 按钮**

在 `web/src/components/AssistantChat/messages/UserMessage.tsx` 中：

1. 从 context 获取 rewind 回调和 session 状态：

```typescript
const seq = useAssistantState(({ message }) => {
    const custom = message.metadata.custom as Partial<HappyChatMessageMetadata> | undefined
    return custom?.seq
})
```

2. 在 JSX 中 Copy 按钮后面新增 rewind 按钮（只在有 seq 且 session 活跃时显示）：

```tsx
{typeof seq === 'number' && ctx.onRewindMessage && (
    <button
        type="button"
        className="flex items-center gap-1 rounded-full px-2 py-1 text-[10px] text-[var(--app-hint)] transition-colors hover:bg-[var(--app-subtle-bg)] hover:text-[var(--app-fg)]"
        onClick={() => ctx.onRewindMessage!(seq)}
    >
        <RewindIcon className="h-3 w-3" />
        <span>Rewind</span>
    </button>
)}
```

3. 需要新增一个简单的 RewindIcon 组件（或使用 Unicode ↩）。为简单起见，用 Unicode：

```tsx
{typeof seq === 'number' && ctx.onRewindMessage && (
    <button
        type="button"
        className="flex items-center gap-1 rounded-full px-2 py-1 text-[10px] text-[var(--app-hint)] transition-colors hover:bg-[var(--app-subtle-bg)] hover:text-[var(--app-fg)]"
        onClick={() => ctx.onRewindMessage!(seq)}
    >
        <span className="text-xs">↩</span>
        <span>Rewind</span>
    </button>
)}
```

- [ ] **Step 4: 在 HappyThread 组件中传递 onRewindMessage 回调**

在 `web/src/components/AssistantChat/HappyThread.tsx` 中，从 `useSessionActions` 获取 `rewindSession`，构造 `onRewindMessage` 回调并传入 context。具体位置需要检查 HappyThread 如何提供 context。

回调逻辑：
```typescript
const onRewindMessage = useCallback(async (targetSeq: number) => {
    if (confirm('回退到这条消息？之后的对话和文件变更将被撤销。')) {
        await rewindSession(targetSeq)
    }
}, [rewindSession])
```

- [ ] **Step 5: 运行类型检查**

Run: `cd /Users/arwen/Desktop/Arwen/evanfang/hapi && bun run typecheck:web 2>&1 | tail -20`
Expected: 无类型错误

- [ ] **Step 6: Commit**

```bash
git add web/src/components/AssistantChat/messages/UserMessage.tsx web/src/components/AssistantChat/context.ts web/src/components/AssistantChat/HappyThread.tsx web/src/lib/assistant-runtime.ts
git commit -m "feat(web): add rewind button to user message bubbles"
```

---

### Task 9: 端到端类型检查和测试

**Files:**
- All modified files

- [ ] **Step 1: 运行全量类型检查**

Run: `cd /Users/arwen/Desktop/Arwen/evanfang/hapi && bun run typecheck 2>&1 | tail -30`
Expected: 无类型错误

- [ ] **Step 2: 运行已有测试**

Run: `cd /Users/arwen/Desktop/Arwen/evanfang/hapi && bun run test 2>&1 | tail -30`
Expected: 所有已有测试通过

- [ ] **Step 3: 运行 rewind 工具函数测试**

Run: `cd /Users/arwen/Desktop/Arwen/evanfang/hapi/cli && bunx vitest run src/claude/utils/rewind.test.ts 2>&1`
Expected: ALL PASS

- [ ] **Step 4: Final commit**

```bash
git add -A
git commit -m "feat: complete rewind implementation across CLI, Hub, and Web"
```
